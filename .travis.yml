language: node_js

node_js:
  - 8

cache:
  directories:
    - node_modules

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y wget

script:
  - npm run build
  - npm test

after_success:
  - chmod +x ./publish.sh
  - ./publish.sh
